import {Component,inject,signal} from '@angular/core';
import {FormsModule} from '@angular/forms';
import {Router,RouterLink} from '@angular/router';
import {Api,errorText} from './api';
import {MATERIAL} from './material';
@Component({standalone:true,imports:[FormsModule,RouterLink,...MATERIAL],template:`<div class="login-page"><div class="login-card"><span class="brand-mark"><mat-icon>health_and_safety</mat-icon></span><p class="eyebrow">CLÍNICA DENTAL</p><h1>Bienvenido de nuevo</h1><p>Ingresa para administrar tu clínica.</p><form (ngSubmit)="login()"><mat-form-field appearance="outline"><mat-label>Correo electrónico</mat-label><input matInput type="email" name="correo" [(ngModel)]="correo" required autocomplete="username"></mat-form-field><mat-form-field appearance="outline"><mat-label>Contraseña</mat-label><input matInput type="password" name="contrasena" [(ngModel)]="contrasena" required autocomplete="current-password"></mat-form-field>@if(error()){<p class="error-banner">{{error()}}</p>}<button mat-flat-button [disabled]="loading()" type="submit">{{loading()?'Ingresando…':'Iniciar sesión'}}</button></form><a mat-button routerLink="/recuperar">Olvidé mi contraseña</a><a mat-button routerLink="/sitio/home">Volver al sitio</a></div></div>`})
export class Login {api=inject(Api);router=inject(Router);correo='';contrasena='';error=signal('');loading=signal(false);login(){this.loading.set(true);this.api.post<{token:string}>('/login',{correo:this.correo,contrasena:this.contrasena}).subscribe({next:d=>{sessionStorage.setItem('clinica-token',d.token);window.location.href='/dashboard';},error:e=>{this.error.set(errorText(e));this.loading.set(false);}});}}
